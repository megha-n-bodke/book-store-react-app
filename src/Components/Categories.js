import React from 'react';

const Categories = () => (
  <div>
    <button type="submit" className="btn btn-primary ms-5 my-5">
      Check Status
    </button>
  </div>
);

export default Categories;
